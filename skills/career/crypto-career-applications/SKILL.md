---
name: crypto-career-applications
description: Portfolio-first strategy for applying to crypto/DeFi roles where production code > traditional resume
trigger: Applying for crypto-native companies (DeFi protocols, trading venues, hackathon-focused teams) where GitHub/mainnet deployments are stronger credentials than formal resumes
version: 1.1.0
---

# Crypto-Native Career Applications Skill

**Date:** 2026-05-04  
**Domain:** Career Strategy for Web3/DeFi Roles  
**Status:** Active

---

## Overview

When applying for crypto-native companies (DeFi protocols, trading venues, infrastructure projects), traditional resumes are secondary to **public, production-grade work**. This skill governs the full application workflow: portfolio evaluation → narrative framing → material preparation → interview strategy for roles where GitHub commits and on-chain deployments speak louder than degrees.

## Credential Stack (as of June 2026)

Jordan's positioning: **smart contracts + security > frontend**. Frontend is commoditized (Cursor can build dashboards). Smart contract development and security auditing are scarce, high-value skills.

**Active credentials in progress:**
- **Cyfrin Updraft** (Patrick Collins / CyfrinAudits) — Solidity course + Security course. Free, self-paced. Completion certificates carry real weight in web3 hiring.
- **Encode Club bootcamps** — Solana bootcamp + Arc bootcamp. Free, self-paced. Completion certificates.
- **CompTIA A+** — Already earned. cybersecurity foundation.
- **Portfolio** — 20 projects, live demos, GitHub Actions deploy (ProtoJay4789.github.io). Primary credential.

**Positioning narrative:** "I'm a smart contract developer with security audit training, production-grade portfolio projects, and verified credentials from top web3 educators. I build the infrastructure this role would use."

**Amazon angle:** Works at Amazon (12hr shifts Thu-Fri). Amazon offers to pay for AWS certification — considered but deprioritized because GenTech is web3-focused, not AI/cloud-focused. May revisit later for full-stack web3 engineer positioning (AWS infra + Solidity + security).

---

## When to Trigger

Apply this skill when:
- User is pursuing **internship, full-time, or contract roles** at crypto companies
- The target is **remote-first, DeFi-native, or trading-focused** (e.g., Hibachi, No Limit Holdings, Uniswap Labs)
- The role emphasizes **technical growth, protocol engineering, or go-to-market engineering**
- User has **production code on-chain or open-source projects** but limited formal resume experience

**Do NOT trigger** for traditional finance (TradFi) or non-tech roles where resume/LinkedIn is still primary.

---

## Core Workflow

### 1. Portfolio-First Assessment
Before any application materials are drafted, audit the user's portfolio with these questions:
- **Production presence:** Are any projects already live on mainnet? (e.g., AgentEscrow, AAE Dashboard)
- **Hackathon momentum:** Are they actively building in public competitions? (ETHGlobal, Solana Frontier, Kite AI)
- **Tech stack match:** Do they have Solidity + Python combinations? (critical for growth engineering)
- **Ecosystem signals:** GitHub contributions graph, fork network, open-source collaboration

**Signal thresholds:**
- ≥1 live mainnet deployment = STRENGTH
- ≥2 active hackathons in progress = MOMENTUM
- Solidity + Python combo = GOLD STANDARD for DeFi roles

### 2. Narrative Framing (Position as "Already Doing the Job")

For crypto-native hiring, frame the candidate as:
> "I'm already building the infrastructure this role would use — I'm just looking to scale it with your team."

**Key narrative pillars:**
- **Production credibility:** "My AgentEscrow is live on Avalanche with real users"
- **Hackathon velocity:** "I'm concurrently building three AAE projects while competing in ETHGlobal"
- **Stack alignment:** "I write smart contracts in Solidity and automate data pipelines in Python — exactly what your SQL/Python growth work needs"
- **Ecosystem embeddedness:** "I'm deep in the Avalanche/Solana ecosystems, not just studying them"

**For role-specific asks (like "Claude expert for SQL/Python"):**
- Tie the portfolio project directly: "My AAE Milestone Tracker automates on-chain data analysis using Python and API calls similar to Claude-assisted workflows"
- Show, don't tell: link to the repo/code rather than listing skills

### 3. Application Material Strategy

**Resume alternative:** Use the GitHub portfolio as the primary credential. If a resume is requested:
- Create a **one-page "Crypto Resume"** that is essentially a portfolio highlight reel
- Front-load production projects with live links and on-chain txn hashes
- Include hackathon track as "Recent Work" section
- Technical skills table: Solidity, Python, DeFi protocols, MCP integrations

**CV Template (portfolio-first format):**
```markdown
# Jordan — AI Agent Builder & Smart Contract Developer

Portfolio: protojay4789.github.io
GitHub: github.com/ProtoJay4789
Focus: AI agents × blockchain × DeFi

## What I Build
[1-2 sentences — position as "already doing the job"]

## Projects
[5-6 projects, each with:]
- What it does (1 line)
- Key metrics (tests passing, holders, deployment status)
- Stack (3-5 technologies)

## Technical Skills
[Table format: category → technologies]

## Hackathon Participation
[Active hackathons with status]

## What Drives Me
[Vision statement — 2-3 sentences about the agent economy]

## Contact
[What roles you're looking for]
```

**Hackathon job application pattern:** Many hackathons (Somnia Agentathon, Encode Club tracks) offer job opportunities alongside prizes. When submitting to a hackathon that has a "would you like to be considered for a job" option:
1. Upload the CV immediately with the hackathon submission
2. The CV should emphasize what you BUILT, not what you studied
3. Link to the live demo + deployed contract + GitHub repo
4. The hackathon submission IS the portfolio piece — double duty

**Pitfall:** Don't write a traditional resume for crypto roles. "Self-taught" is a strength in this space — it signals you built because you wanted to, not because a class required it.

**Cover letter approach:**
- Opening: "After reviewing your growth role, I noticed you're looking for someone with SQL/Python automation experience — my AAE Milestone Tracker does exactly that for real on-chain positions"
- Middle: "I'm not just studying DeFi, I'm shipping in it — AgentEscrow is already handling X txns, and I'm actively competing in Y hackathons"
- Close: "My Amazon flex schedule gives me the flexibility to deliver growth work on US/EU timezones while maintaining my own DeFi projects"

### 4. Interview Prep Focus

For technical interviews at crypto companies:
- **Be ready to walk through production code:** Have 2-3 repos selected for deep dives
- **Know your on-chain metrics:** TVL, transaction counts, user addresses for live projects
- **Hackathon demos:** Have live demos or video walkthroughs ready for current projects
- **Ecosystem fluency:** Understand the target company's protocols (e.g., if Hibachi trades on Avalanche, know your AVAX DeFi landscape)
- **Python/SQL pairing:** Prepare to discuss data pipelines that combine on-chain data with off-chain analytics

---

## Pitfalls to Avoid

1. **Academic overemphasis:** Don't lead with coursework, degrees, or school projects. Crypto hiring cares about shipped code.
2. **Vague claims:** Never say "experienced in DeFi" without pointing to a live protocol or audit.
3. **Resume-only submissions:** If they ask for a resume, always attach the portfolio link prominently. The portfolio is the main story.
4. **Ignoring hackathon momentum:** Active hackathon participation is a SIGNAL of passion, not a distraction — frame it as concurrent shipping.
5. **Underplaying Amazon flex:** The remote-flex arrangement is a STRENGTH for internships — it means no relocation cost, immediate availability, and low turnover risk.

---

## Application Template Snippets

**Email subject line:**
```
[Hibachi Growth Intern Application] — Jordan Jones (ProtoJay4789)
Crypto-native growth candidate with production DeFi experience
```

**Opening paragraph template:**
> I'm applying for the Hibachi Growth Intern role — your mention of "Claude expert for SQL/Python" caught my eye because I'm already using Python automation to track real on-chain positions (AgentEscrow + AAE Dashboard). I'm actively shipping in the Avalanche DeFi ecosystem while competing in ETHGlobal Open Agents and Solana Frontier.

**Portfolio summary paragraph:**
> My GitHub (protojay4789) shows production-grade work: AgentEscrow (live on-chain), AAE Dashboard (real yield tracking with milestone system), and cross-chain adapters. I'm concurrently building three autonomous agent projects in ETHGlobal and two Solana hackathons — the same velocity I'd bring to Hibachi's growth initiatives.

---

## Career Scanner Cron

**Existing Cron Job:** "5-Star Opportunity Scanner — Hackathons + Jobs + Grants" (job_id: 71d5c3e3b245)
- Skills: `hackathon`, `opportunity-discovery`, `crypto-career-applications`
- Schedule: Mon/Thu 10 AM
- Delivers to: origin
- **Status check:** Run `hermes cron list` to see last run status

**If scanner errors:** Common issues:
1. **web_search blocked** — Requires FIRECRAWL_API_KEY → Run `hermes model` → Configure at https://portal.nousresearch.com/billing
2. **web3.career** — Works via curl + JSON-LD extraction (see Tool Preference section below). No browser needed.
3. **crypto.jobs** — Server-rendered HTML; curl + text extraction finds job titles, companies, posting ages. Not fully structured but usable.
4. **Devpost, DoraHacks, ETHGlobal** — JS-rendered SPAs. Curl returns skeleton only. Colosseum's SvelteKit SSR embeds event data in `__sveltekit` JSON within HTML (regex-extract `currentEvent` object).
5. **Site maintenance** — web3.career goes down periodically; skip gracefully and note in report.

**Design principle: "Silent when nothing found."** The scanner only delivers results if it finds genuine 4-5 star matches. If nothing meets the quality threshold, it sends nothing. This prevents spam and ensures Jordan only sees roles worth his time.

**Match criteria (ALL must be true):**
1. **100% REMOTE — work from anywhere in the world** (not hybrid, not remote-US-only, not remote-with-travel-restrictions, not "remote with quarterly on-site"). Jordan travels full-time (Thailand, Philippines). Fully distributed, async-friendly teams ONLY.
2. No "3+ years experience" requirement (entry-level, 0-3 years)
3. Matches Jordan's tech stack (Solidity, Python, agents, DeFi)
4. At crypto/web3/AI companies (not TradFi, not enterprise)
5. Currently accepting applications (not expired)

**⚠️ VALIDATION RULES (MANDATORY — from Jordan, Jun 2026; updated Aug 18):**
0. **CHECK THE DATE FIRST (Jordan Aug 18 2026 — HIGHEST PRIORITY GATE).** Before getting
   excited about ANY role, verify when it was posted. If the listing is outdated, closed,
   expired, or older than ~60 days, SKIP IT and move on — do NOT report it as live and do
   NOT draft an application. Dead/outdated listings reported as live wasted everyone's
   time (e.g. Openmesh "Junior API Developer" was closed since May 2024 yet initially
   hyped as a perfect fit). Judge freshness from: the posting date shown, "closed"/"expired"
   indicators, "30+ days ago", and whether the official careers/recruitee/wellfound page
   still lists it as open. Freshness beats fit — a perfect-fit role that is dead is worthless.
1. **Verify the job is still active** — check posting date, look for "closed" or "expired" indicators. If the link 404s or says "no longer accepting applications", SKIP IT. Do not report dead listings.
2. **Verify "remote" actually means remote** — some jobs say "remote" but require Asia-based candidates (China, Taiwan, etc). If the company is primarily Asia-based or the listing mentions specific Asian time zones, SKIP IT even if it says "remote."
3. **Check web3.career separately** — it may be down for maintenance. If so, skip and note it in the report. Don't assume it's always available.
4. **Don't reuse previously found jobs** — check session history to avoid duplicates across weekly scans.
5. **Binance is Asia-based** — even their "remote" roles require Asia location. Skip all Binance positions unless explicitly confirmed as worldwide remote.

**Scoring:**
- ★★★★★ = Perfect match (agent + blockchain + DeFi + remote + entry-level)
- ★★★★☆ = Strong match (4/5 criteria met, good growth potential)
- ≤★★★☆☆ = Skip (don't deliver)

### Scanner Execution Notes

**Tool workarounds (discovered May–Jul 2026, updated for MCP Exa + GLM-5.2):**
- `web_search` BLOCKED (no FIRECRAWL_API_KEY). Use **MCP Exa via BlockRun** instead.
- `web_extract` does NOT work with DuckDuckGo backend (search-only).
- **Primary method:** MCP Exa (`mcp_blockrun_blockrun_exa`) for job board search + extraction.
- **Exa `search` endpoint:** Find job listing pages ($0.01/call). Query: `"<tech stack> remote jobs site:<job_board>"`.
- **Exa `contents` endpoint:** Extract job listings from found URLs ($0.002/URL, up to 100).
- **Exa `find-similar` endpoint:** Find similar jobs to a promising listing ($0.01/call).
- GLM-5.2's strength: Better relevance scoring from extracted content, smarter fallback strategies.
- `cryptojobslist.com` — Cloudflare blocked. Skip entirely.
- `wellfound.com` — CAPTCHA blocked. Skip.
- `himalayas.app` — Cloudflare blocked. Skip.
- `binance.com/en/careers` — SPA. Use Exa `contents` on Lever portal (`jobs.lever.co/binance/`) if needed.
- `web3.career` — Works via Exa. Search → contents → extract job cards from text.
- `crypto.jobs` — Search finds career guides. Need direct category URLs (e.g., `https://crypto.jobs/jobs?q=python&location=remote`).

**Source reliability ranking (verified Jul 2026 with MCP Exa):**
1. `web3.career` — Exa `search` + `contents` works well. 780+ remote AI jobs. Extract job cards from text.
2. `crypto.jobs` — Need direct category URLs for Exa `contents`. 3500+ listings.
3. `Remotive.com` — Check category-specific URLs. Exa `search` finds pages.
4. `sailonchain.com` — Low confidence. Use as backup only.
5. `jobs.lever.co` — Company ATS portals. Exa `contents` works reliably.
6. `cryptojobslist.com` — Blocked. Skip.
7. `wellfound.com` — Blocked. Skip.
8. `himalayas.app` — Blocked. Skip.
9. `workatastartup.com` — URLs decay. Verify listing is still live.
10. `LinkedIn` — Use Exa `search` for discovery only.

**Market intelligence (June 2026):**
- AI agent hiring is real but **heavily skewed senior** (Principal/Staff at OKX, Zscaler). Entry-level AI agent roles are rare.
- The "AI Agent Engineer" title is now standard at major exchanges (Binance, OKX) — validates Jordan's positioning.
- **Jordan is entry-level (0-3 years)** — not mid, not senior. Target roles that say "junior", "entry-level", or don't specify years.
- Most crypto AI roles require Python + LLM experience — Jordan's Python automation stack is directly relevant.
- "Hackathon experience" listed as bonus on multiple listings — frame concurrent hackathon participation as a strength.
- Roles requiring "production TypeScript + Postgres + React" are a poor fit — Jordan's strengths are Python, Solidity, and AI agent architecture.

## Tracking & Follow-up

After submission, track:
- Application date + contact
- GitHub/portfolio views (set up notifications if possible)
- Follow-up cadence (7-day interval max)
- Interview stages and prep notes

Store follow-ups in: `04-Entertainment/Applications/{Company}-{Role}-tracker.md`

---

## GLM-5.2 Job Extraction Pattern

When web_search is blocked, use curl + Python regex extraction with GLM-5.2 for relevance scoring.

### Step 1: Fetch HTML via curl
```bash
curl -s -A "Mozilla/5.0" "https://crypto.jobs/jobs" > /tmp/crypto-jobs.html
curl -s -A "Mozilla/5.0" "https://web3.career/remote-jobs" > /tmp/web3-career.html
```

### Step 2: Extract job cards via Python regex
```python
import re
from html import unescape

# crypto.jobs: Look for job cards
card_pattern = r'<a[^>]*class="[^"]*job[^"]*"[^>]*>(.*?)</a>'
cards = re.findall(card_pattern, html, re.IGNORECASE | re.DOTALL)

# Extract title, company, tech stack, remote status
for card in cards:
    title_match = re.search(r'<h3[^>]*>(.*?)</h3>', card, re.IGNORECASE | re.DOTALL)
    company_match = re.search(r'class="company"[^>]*>(.*?)</span>', card, re.IGNORECASE | re.DOTALL)
    remote_match = re.search(r'Remote|remote|Worldwide|worldwide', card)
    
    # Clean and structure data
```

### Step 3: GLM-5.2 relevance scoring
Use GLM-5.2 to score jobs against Jordan's criteria:
- 100% remote (no timezone constraints)
- Entry-level (0-3 years)
- Python + Solidity stack
- Crypto/web3/AI companies

### Step 4: Filter and deliver
Only deliver 4-5 star matches. Silent if nothing found.

**Pitfalls:**
- crypto.jobs: HTML is server-rendered (~235KB). Curl + text extraction (strip scripts, extract text) finds job titles, companies, "X days ago" stamps, and salaries from the plain text. Works but less structured than JSON-LD.
- web3.career: **DOES work via curl** — the listing page returns full server-rendered HTML with **JSON-LD JobPosting data** in `<script type="application/ld+json">` blocks. Each block has title, company, datePosted, salary, remote status, applicant requirements, and full description. Extract with `python3 -c "import re, json; ..."`. See `references/web3-career-jsonld-extraction.md` for the complete extraction script.
- Colosseum (colosseum.org): SvelteKit SSR embeds event data inside the HTML. Regex-extract `__sveltekit` JSON for `currentEvent` objects (eventType, name, phase, countdownTarget, participantCount). Works without JS rendering.
- Always validate: check if link 404s, check posting date, verify "remote" isn't Asia-only.
- **Do NOT use web_search** — blocked on this system. MCP Exa or curl + JSON-LD are replacements.

### Tool Preference: curl + JSON-LD ≈ MCP Exa > web_search

| Task | Primary Tool | Fallback | Cost |
|------|-------------|----------|------|
| web3.career job scraping | **curl + JSON-LD extraction** | MCP Exa `contents` | Free (curl) |
| crypto.jobs scraping | **curl + text extraction** | MCP Exa `search` | Free (curl) |
| Colosseum hackathon data | **curl + sveltekit JSON extraction** | Browser | Free (curl) |
| Other SPAs (Devpost, DoraHacks) | MCP Exa (`mcp_blockrun_blockrun_exa`) | Wait for recovery | ~$0.007/search |
| Company research | MCP Exa | None | ~$0.007/search |

**Why curl + JSON-LD for web3.career?**
- 15 JobPosting entries per page, fully structured (title, company, salary, location, date, description)
- No API keys needed, no payment required
- Works even when MCP Exa wallet is unfunded or BlockRun is down
- The JSON-LD includes `applicantLocationRequirements` and `jobLocationType` for remote verification
- Also works on tag-filtered pages (`/solidity+remote-jobs`, `/dev+entry-level-jobs`, `/intern-jobs`)

**How it works (web3.career):**
```bash
curl -s -L -A "Mozilla/5.0" "https://web3.career/remote-jobs" > /tmp/web3-jobs.html
python3 -c "
import re, json
with open('/tmp/web3-jobs.html') as f:
    html = f.read()
pattern = r'<script type=\"application/ld\+json\">(.*?)</script>'
for m in re.findall(pattern, html, re.DOTALL):
    data = json.loads(m.strip())
    if data.get('@type') == 'JobPosting':
        print(f\"{data.get('title')} @ {data.get('hiringOrganization',{}).get('name')} | {data.get('datePosted','')}\")
"
```

**How it works (Colosseum):**
```bash
curl -s -L "https://www.colosseum.org/hackathons" | python3 -c "
import re, sys
html = sys.stdin.read()
match = re.search(r'currentEvent:({.*?}})', html)
if match: print(match.group(1))
"
```

**If MCP Exa is down:**
- Retry with exponential backoff (2s, 4s, 8s)
- web3.career and crypto.jobs: curl already works as primary
- Colosseum: curl + sveltekit extraction works
- Devpost/DoraHacks/ETHGlobal: these are true SPAs — curl returns skeleton. No good curl workaround for Devpost (even the API returns empty). ETHGlobal page text embed event names in plain text within the <body> even without JS, regex-extractable.

---

## References

- `references/career-scanner-cron.md` — Weekly cron job pattern, search sources, scoring criteria, "silent when nothing found" principle
- `references/career-scanner-validation.md` — Validation rules from Jordan (verify active, verify remote, no duplicates, Binance = Asia)
- `references/web3-career-jsonld-extraction.md` — Complete extraction script for web3.career's JSON-LD JobPosting data
- `references/web-search-fallback-patterns.md` — Curl-based fallbacks when web_search and MCP Exa are unavailable
- Hibachi Growth Intern JD (College.xyz, May 2026)
- User's portfolio: https://protojay4789.github.io/
- GitHub: https://github.com/protojay4789
- GenTech vault: `/root/vaults/gentech/03-Projects/AAE/` (milestone tracker, regime classifier)

---

## Support Files

**References:**
- `references/hibachi-growth-intern-2026.md` — Role context and user credential mapping
- `references/web3-career-jsonld-extraction.md` — curl + JSON-LD extraction scripts for web3.career job data
- `references/web-search-fallback-patterns.md` — Curl-based fallbacks for blocked web_search

**Templates:**
- `templates/cover-letter-portfolio-first.md` — Reusable cover letter skeleton for crypto-native applications

**Scripts:**
- None currently